#include "settings.hpp"
#include "utils/magic_enum.hpp"


void settings::update()
{
    // const settings& c = curr();
    // settings& p       = prev();
    // if (c.transpose != p.transpose) {
    //     // Update transpose.
    //     // speaker::on_transpose(c.transpose);
    //     p.transpose = c.transpose;
    //     p.autochord = c.autochord;
    // }
    // p = c;
}
